name: one-time fix ceremony mobile layout
on:
  push:
    paths:
      - '.github/workflows/one-time-fix-ceremony-mobile-layout.yml'
      - 'scripts/one-time-fix-ceremony-mobile-layout.py'
permissions:
  contents: write
jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply patch
        run: python3 scripts/one-time-fix-ceremony-mobile-layout.py
      - name: Run tests
        run: npm test
      - name: Commit production changes and remove one-time files
        run: |
          rm -f scripts/one-time-fix-ceremony-mobile-layout.py .github/workflows/one-time-fix-ceremony-mobile-layout.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: restore compact ceremony controls on mobile'
          git push
